# Supabase integration test configuration
# Copy this file to test/.env and fill in your test Supabase credentials.
# The .env file is gitignored and will NOT be committed.
#
# IMPORTANT: Use a dedicated test user / test project to avoid polluting production data.
# Tests use table prefix "test_" (e.g. test_diary_days, test_notes, test_note_templates).
# Ensure these tables exist in your Supabase project — see supabase.sql.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_TEST_EMAIL=test@example.com
SUPABASE_TEST_PASSWORD=your-test-password
